#include <stdio.h>             // Include the library that contains the command printf()

int main() {                   // Fonction principale : c’est le point d’entrée du programme
  printf("Hello, World!");     // Affiche le texte "Hello, World!" dans le terminal
  return 0;                    // Indique que le programme s’est terminé sans erreur
}